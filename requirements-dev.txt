# Development dependencies for local pre-commit hooks.
# Python 3.12 is required (ansible-lint is incompatible with 3.14 today).
#
# Setup:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-dev.txt
#   pre-commit install

pre-commit>=4.0.0
ansible-lint==25.1.3
ansible-core>=2.16.0,<2.19.0
