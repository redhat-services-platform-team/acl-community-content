---
# Galaxy collections baked into the ACL community content EE.
# Keep in sync with collections/requirements.yml.

collections:
  - name: ansible.posix
    version: ">=2.1.0,<3.0.0"
  - name: ansible.windows
    version: ">=2.0.0,<3.0.0"
  - name: community.general
    version: ">=8.0.0,<13.0.0"
